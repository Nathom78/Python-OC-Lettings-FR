[flake8]
max-line-length = 99
exclude =
	**/migrations/*,
	venv,
	.git,
	__pycache__,
	.env/,
	.idea/,
	static/*,
	.pytest_cache/,
	reports/,
	docker_image.yml
tee = True
output-file = ./reports/flake8/flake8stats.txt
format= html
statistics = True
htmldir = ./reports/flake8
show_source = True

[tool:pytest]
DJANGO_SETTINGS_MODULE = oc_lettings_site.settings
python_files = tests.py
addopts = -v --no-migrations

[coverage:run]
omit = tests.py, reports/, static/, staticfiles/
data_file = "reports/coverage/.coverage"


[coverage:report]
omit = tests.py, reports/, static/, staticfiles/
fail_under = 80
data_file = "reports/coverage/.coverage"


[coverage:html]
directory = reports/coverage
data_file = "reports/coverage/.coverage"
fail_under = 80

[coverage:xml]
output = reports/coverage
data_file = "reports/coverage/.coverage"
fail_under = 80
